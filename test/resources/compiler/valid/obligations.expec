function save(snapshot: Text) {
  promises "Save the snapshot to durable storage."
}
examples {
  action saveSnapshot(snapshot: Text) returns Nothing
  observation savedSnapshot() returns Text
  check expectSaved(snapshot: Text)
  scenario "the prose remains unfinished" {
    when saveSnapshot("first")
    then satisfies "The saved snapshot survives a restart."
  }
}
